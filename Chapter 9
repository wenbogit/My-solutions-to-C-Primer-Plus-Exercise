#include <stdio.h>
#include <stdlib.h>

/*9.1
double min(double a, double b);

int main(void)
{
    double x, y, z;

    printf("Please enter two numbers: \n");
    scanf("%lf%lf", &x, &y);
    printf("The smaller one is %.2f\n", min(x,y));


    return 0;
}

double min(double a, double b)
{
    double min;
    if (a>b)
        min=b;
    else
        min=a;

    return min;
}
*/
